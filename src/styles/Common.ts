import styled from 'styled-components';

export const Container = styled.div`
  border: 1px solid red;
  font-size: 16px;
  font-weight: 400;
  line-height: 1.5;
`;

export const ImgCommon = styled.img`
  display: block; //이미지들은 블록으로 만들어서 밑에 글씨로 인해 남는 여백을 없애주기위해
`;
